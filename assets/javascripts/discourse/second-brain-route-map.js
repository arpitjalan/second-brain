// A dedicated page for searching your chats (reachable from the sidebar link and a
// subtle homepage link), so the homepage launcher stays clutter-free. The route
// name "search-chats" gives the path /search-chats.
export default function () {
  this.route("search-chats");
}
